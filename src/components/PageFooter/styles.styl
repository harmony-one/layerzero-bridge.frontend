.footer {
  position: fixed;
  left: 0;
  bottom: 0;
  width: 100%;
  height: 4em;
}

.footerDot {
  margin-bottom: 6px;
  font-size: 1.5rem;
}

.footerText {
  p {
    font-size: 0.6rem;
    font-weight: 100;
  }
}

